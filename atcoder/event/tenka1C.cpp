#include <iostream>
#include <cmath>
int main (void) {
    cin >> N;
    for (int n = 1 ; n <= 3500 ; n ++ ) {
        for (int h = 1 ; h <= 3500 ; h ++ ) {

            w = n * h * N / (4 * n * h - N * n - N * h)
            
            if ( ceil(w) == floor(w)) break;

